python

一、运算
1.#算数运算符
直接eg:print(1+1) , print(1/1) , print(1*1) , print(1-1)  #使用算数运算符/，商一定是浮点数且除数不为0
 使用算数运算符，若是a、b里面某一位是浮点数则结果也是浮点数
 
# // 两个斜杠 取整除 取商的整数部分，向下取整，不管四舍五入的规则只要后面有小鼠，就忽略小数
eg：print（a//b）

# % 取余数 只取余数部分
eg：print（a%b）  如果a/b是5/2=2...1 则这个数值等于1

# ** 幂 m**n：m的n次方
print（a**b)

优先级排序：幂（最高优先级）>乘、除、取余、取整除>加减

2、赋值运算符
#给变量赋值
num1=5  num2=8
将变量的值赋给另外一个变量
num3=num1  print（num3）输出为5
total1=num1+num3  print（total) 输出为10

# +=  -=
a=1 print(a)输出为1
重新赋值a=a+1
书写为：a+=1 等效于a=a+1  #赋值运算符号必须连着写中间不能有空格，赋值运算符号就是+=和-=这个整体的左右两边可以空格

3、比较运算符（关系运算符）
==   比较两个变量的值是否相等，相等的话就返回为true（真），不相等就返回为false（假）
！=  比较两个变量的值是否相等，不相等的话就返回为true（真），相等就返回为false（假）
> < >= <=

4、逻辑运算符
#and 左右两边都符合才为真
eg：
a=“哈哈”
b="hh"
ifa==“哈哈”and b="hh"
  print("a和b都在笑）
#a、b对应文字以及上面的赋值都没问题时，文字输出，若其中某一处b="hh"变成了a="hh"将不会输出

#or 左右两边只需要符合一边就为真

#not 表示相反的结果 
eg：print（not 3>9) 输出为ture  #但实际3是小于9的这个答案是错的

5、三目运算（三元表达式）
#if-else     #可写多个else
 if 条件：
    满足条件时要做的事情
 else：
    不满足条件要做的事情
 eg:
  a=66
  ifa==999
    print（“你真棒”)
  else:
    print("还要继续加油”）

#三目运算
a=5  b=8   #实则这个运算是关注ab的值的运算
if a<=b:
    print（“a小于等于b”)    #这个是真结果
else:
   print("a比b大")          #这个是假结果
print（“a小于等于b”）if a<=b else print（“a比b大”）
输出为a小于b              #由上方ab的赋值可知

#if嵌套
  #定义一个布尔型变量，表示是否有车票
ticket=ture  #ture代表有车票，false表示无车票
  #定义一个福店型变量
temp=36.5
  #判断是否有车票
if ticket==ture:   #外层if判断
   print（“可以进站了”）
       #正常人体温是36.3-37.2
   if 36.3<=temp<= 37.2    #内层if判断
      print("体温正常可以回家")
   else：
      print（"体温不正常，被拉去医院"）   
else：
   print（“没票不能进站”）

二、输入函数
#input（）输入函数
input（prompt） prompt是提示词，会在控制台显示
eg：name=input（“请输入名字：”)
输出为： 请输入名字：      #此时你可以填写自己的名字，对name进行一个赋值
eg：我进行一个名字填写：  请输入名字：huahua    #此时系统对name这个词赋值成功，接下来需要一个展示
eg：name=input（“请输入名字：”)
    print（name）         #这时你对name赋值后回车，会出现你填写的内容，类似于取一个游戏id，填写好后展示给你自己看

三、转译字符
1、 \t 制表符  通常表示空四个字符，也称缩进
eg:print(name\tage)
输出为 name    age

2、\n 换行符  表示将当前位置移到下一行开头

3、 \r 回车 表示将当前位置移到本行开头  #前面的内容就不存在了

4、 \\ 反斜杠符号  表示一个斜杠在文中显示  #在引号前方加上r原生字符串，默认取消转译eg：print（“a//b) 输出为 a/b  加入r后：print（r“a//b）输出为a//b


四、if判断
1、if判断 #运用场景：判断是否为成年人、判断密码是否正确等
eg：判断是否能上网
age=21
ifage<18：#必须打冒号！！！）
  print("未成年不能上网”）   #前方需要缩进不然会报错

四、循环语句
 1、循环语句
  




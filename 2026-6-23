一、LLM应用开发专有名词解释：
*LLM大语言模型：通过自监督学习或半监督学习在大量无标签文本上进行训练
*AIGC以适当的泛化能力生成相关内容的技术，涵盖文字、图像、视频、音频、游戏、虚拟人等
*AGI人工通用智能
*Agent智能代理：能够自主感知环境并采取行动的计算实体
*Prompt提示词
*GPT生成型预训练变换模型
*Token文本基础单元/词元
*LoRA插件式微调
*矢量/向量数据库
*数据蒸馏：复杂模型转化为更轻量级的模型，提高模型的鲁棒性和泛化能力

二、LLM和AI Agent应用交互方式
 -嵌入模式：AI是执行工具，人类是决策者和指挥者
 -副驾驶模式：互补，AI提供意见并协助任务
 -智能体模式：AI独立完成大部分工作，人类监督和评价结果

三、Python（是面向对象的解释型高级编程语言、是强类型的动态脚本语言）
#运行方式
 1右击空白处“run py01”，出现控制器console底部有一个run
 2或者点击左侧边栏的运行图标等

#错误情况
 1输入错误
 2缩进错误
 3语法错误
 4命名错误：字母书写时为字符串（变量），必须加上引号，单引号或者双引号，数字被直接当作整数类，不会当作变量

#Debug
 设置断点，可以进行debug调试
几个功能：
 1、show execution point 显示执行的断点，无论你的鼠标在哪都跳转到debug最前面的断点
 2、step into 下一步
 3、Run to cursor 跳到下一个断点的位置
 ·在debug区间，代码为蓝色背景时表示即将要运行的代码

#注释
单行注释 直接使用“#”可以放在代码的前面也可以放在后面
多行注释 用三引号包含，可以是三队单引号也可以是三队双引号
·ctrl＋斜杠 可以直接添加注释，可以添加也可以取消注释，使用的是单行注释
 ctrl＋d复制到下一行

#输出
输出多个值或者多句话时用英文逗号隔开，显示为话与话之间有空格
print（“哈哈哈”，“呵呵呵”，“嘿嘿嘿”）显示为：哈哈哈 呵呵呵 嘿嘿嘿 
sep时一个分隔符，需要所有的值写完后再写，它可以设置句子之间的符号，间隔多个值 eg：print（“哈哈哈”，“呵呵呵”，“嘿嘿嘿”sep=“。”）显示为：哈哈哈。呵呵呵。嘿嘿嘿 
end用来设定以...结尾，默认值是换行符，中间可以用任何字符串、符号链接两个语句 eg：print（字符串，end=“后面拼接的词”）最后输出结果：第一个print中的字符串＋后面拼接的词加第二个print中的字符串

#变量
·“=”是赋值运算符，左右两边打上空格是为了代码的规范性和美观性
eg：赋值a=666 那么a被赋值为666，由于代码运行是从上到下，所以同一个变量可以被反复赋值，且可以为不同数据类型，eg下一行重新赋值a=999则print（a）输出变为999

#标识符规定（定义的变量名、函数名）
1、只能由数字、字母、下划线组成
eg上方的赋值变量名a，可以修改为aa、1a、a_,这几种情况
2、只能是包含在（）内的标识
3、python已经使用了的标识符，具有特殊的功能和含义，不能作为标识符，就是不能用于赋值
4、严格区分大小写

#数值类型
1、整数int（正负数）
2、浮点型float（小数）
3、布尔型bool(有固定写法，一个为True<真>，一个False<假>注意大小写，布尔值可以当作整型对待，true相当于整数1，false相当于整数0，通常用于判断）
4、复数complex（固定写法：z=a + bj --a是实数，b是虚数部，j是虚数单位部，只能用j表示
·检测数据类型的方法type()

#格式化输出
1、占位符
  ·%
   %s 字符串 常用，
   %d 整数    
   %nd 整数，n表示位数如果数字赋值三位数，n=3，那输出前面不具有空格，如果n=6，输出的字符前面有三个字符大小的空格，其中，在n前面再加上一个0，则可以用0来补齐前面多出来的字符空格，只有0可以
   表示占用的号，意思是要占这个位置，后面的%后的内容就是要去占这个位置的东西
   %f 浮点数（小数）
  eg:age=18
     name='huahau'
    print("我的名字：%s，年龄：%d"%(name,age)
则输出为：我的名字：huahua,年龄：18
  eg2：a=123
     print("05d"%a)
输出为00123
  eg3：b=2.34567
     print（“%.3f”%b)
输出为2.345 ，一定要在%后面加 . ,它后面的数字表述保留的小数点位数，遵循四舍五入，默认显示七位数，不够可能用0补齐

2、f格式化
  格式：f"{表达式}"
  eg：
    name=huahua
    age=18
  print（f“My name is {name}，i am {age} years old.")
输出为 My name is huahua，i am 18 years old.
